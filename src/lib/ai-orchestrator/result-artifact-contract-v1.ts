import { z } from 'zod';
import { canonicalJson, canonicalSha256, sha256 } from '../canonical-json';
import { FAI_AUDIT_JOB_CODES, FAI_AUDIT_JOB_DEFINITION_HASHES, type FaiAuditJobCode } from './job-catalog-v1';
import { AI_ORCHESTRATOR_WORKER_CAPABILITY_HASHES, AI_ORCHESTRATOR_WORKER_RUNTIME_POLICY_HASH, getAiOrchestratorWorkerCapability } from './worker-runtime-policy-v1';

export const AI_RESULT_CONTRACT_VERSION = '1.0' as const;
export const AI_RESULT_CONTRACT_CATALOG_CODE = 'FAI-AUDIT-RESULT-ARTIFACT-CONTRACT-CATALOG' as const;
export const AI_RESULT_LIMITS = Object.freeze({ maxArtifacts: 8, maxArtifactBytes: 16 * 1024, maxResultBytes: 64 * 1024, maxSourceReferences: 16, maxJsonDepth: 8, maxJsonNodes: 512, maxStringBytes: 4096, maxCodeBytes: 120 } as const);
const FORBIDDEN = /(<\/?[a-z][\s\S]*>|https?:\/\/|file:\/\/|\b(prompt|secret|password|token|api[_-]?key|crm real|cliente reale)\b)/i;
const HASH_RE = /^[0-9a-f]{64}$/;
const code = z.string().regex(/^[A-Z0-9_]{1,120}$/).superRefine((value, ctx) => { if (Buffer.byteLength(value, 'utf8') > AI_RESULT_LIMITS.maxCodeBytes) ctx.addIssue({ code: z.ZodIssueCode.custom, message: 'AI_RESULT_CODE_TOO_LARGE' }); });
const logicalKey = z.string().regex(/^[A-Za-z0-9_.:-]{1,120}$/);
const syntheticText = z.string().superRefine((value, ctx) => { if (Buffer.byteLength(value, 'utf8') > AI_RESULT_LIMITS.maxStringBytes) ctx.addIssue({ code: z.ZodIssueCode.custom, message: 'AI_RESULT_STRING_TOO_LARGE' }); if (FORBIDDEN.test(value)) ctx.addIssue({ code: z.ZodIssueCode.custom, message: 'AI_RESULT_FORBIDDEN_CONTENT_REDACTED' }); });
const score = z.number().min(0).max(1).finite();
const base = z.object({ synthetic: z.literal(true), summary: syntheticText }).strict();
const review = base.extend({ passed: z.boolean(), issues: z.array(syntheticText).max(16) }).strict();
const schemas = Object.freeze({
  DOCUMENT_MANIFEST: base.extend({ documentCount: z.number().int().min(0).max(100) }).strict(),
  DOCUMENT_CLASSIFICATION: base.extend({ classes: z.array(syntheticText).max(16) }).strict(),
  EVIDENCE_SET: base.extend({ evidenceItems: z.array(syntheticText).max(64) }).strict(),
  FINANCIAL_ANALYSIS: base.extend({ indicators: z.array(z.object({ code: syntheticText, value: z.number().finite() }).strict()).max(32) }).strict(),
  CREDIT_ANALYSIS: base.extend({ rating: syntheticText, confidence: score }).strict(),
  CALCULATION_SET: base.extend({ calculations: z.array(z.object({ code: syntheticText, value: z.number().finite() }).strict()).max(64) }).strict(),
  FINDINGS_DRAFT: base.extend({ findings: z.array(syntheticText).max(32) }).strict(),
  REPORT_DRAFT: base.extend({ sections: z.array(syntheticText).max(32) }).strict(),
  SCHEMA_REVIEW_REPORT: review,
  NUMERIC_REVIEW_REPORT: review,
  SOURCE_REVIEW_REPORT: review,
  RED_TEAM_REVIEW_REPORT: review,
  CORRECTED_REPORT: base.extend({ correctedSections: z.array(syntheticText).max(32) }).strict(),
  CORRECTION_MANIFEST: base.extend({ correctionReasons: z.array(syntheticText).max(32), supersededArtifactHashes: z.array(z.string().regex(HASH_RE)).max(16) }).strict(),
});
type ArtifactType = keyof typeof schemas;
const contractDefs = Object.freeze({
  DOCUMENT_INGESTION: ['DOCUMENT_MANIFEST'], DOCUMENT_CLASSIFICATION: ['DOCUMENT_CLASSIFICATION'], EVIDENCE_EXTRACTION: ['EVIDENCE_SET'], FINANCIAL_ANALYSIS: ['FINANCIAL_ANALYSIS'], CREDIT_ANALYSIS: ['CREDIT_ANALYSIS'], CALCULATIONS: ['CALCULATION_SET'], FINDINGS_DRAFTING: ['FINDINGS_DRAFT'], REPORT_COMPOSITION: ['REPORT_DRAFT'], SCHEMA_REVIEW: ['SCHEMA_REVIEW_REPORT'], NUMERIC_REVIEW: ['NUMERIC_REVIEW_REPORT'], SOURCE_REVIEW: ['SOURCE_REVIEW_REPORT'], RED_TEAM_REVIEW: ['RED_TEAM_REVIEW_REPORT'], CORRECTION: ['CORRECTED_REPORT', 'CORRECTION_MANIFEST'],
} satisfies Record<FaiAuditJobCode, readonly ArtifactType[]>);
const schemaDescriptions = Object.freeze({
  DOCUMENT_MANIFEST: { synthetic: 'literal:true', summary: 'syntheticText', documentCount: 'int:0..100' }, DOCUMENT_CLASSIFICATION: { synthetic: 'literal:true', summary: 'syntheticText', classes: 'syntheticText[]<=16' }, EVIDENCE_SET: { synthetic: 'literal:true', summary: 'syntheticText', evidenceItems: 'syntheticText[]<=64' }, FINANCIAL_ANALYSIS: { synthetic: 'literal:true', summary: 'syntheticText', indicators: '{code,value}[]<=32' }, CREDIT_ANALYSIS: { synthetic: 'literal:true', summary: 'syntheticText', rating: 'syntheticText', confidence: 'number:0..1' }, CALCULATION_SET: { synthetic: 'literal:true', summary: 'syntheticText', calculations: '{code,value}[]<=64' }, FINDINGS_DRAFT: { synthetic: 'literal:true', summary: 'syntheticText', findings: 'syntheticText[]<=32' }, REPORT_DRAFT: { synthetic: 'literal:true', summary: 'syntheticText', sections: 'syntheticText[]<=32' }, SCHEMA_REVIEW_REPORT: { synthetic: 'literal:true', summary: 'syntheticText', passed: 'boolean', issues: 'syntheticText[]<=16' }, NUMERIC_REVIEW_REPORT: { synthetic: 'literal:true', summary: 'syntheticText', passed: 'boolean', issues: 'syntheticText[]<=16' }, SOURCE_REVIEW_REPORT: { synthetic: 'literal:true', summary: 'syntheticText', passed: 'boolean', issues: 'syntheticText[]<=16' }, RED_TEAM_REVIEW_REPORT: { synthetic: 'literal:true', summary: 'syntheticText', passed: 'boolean', issues: 'syntheticText[]<=16' }, CORRECTED_REPORT: { synthetic: 'literal:true', summary: 'syntheticText', correctedSections: 'syntheticText[]<=32' }, CORRECTION_MANIFEST: { synthetic: 'literal:true', summary: 'syntheticText', correctionReasons: 'syntheticText[]<=32', supersededArtifactHashes: 'sha256[]<=16' },
} satisfies Record<ArtifactType, Record<string, string>>);
export const AiResultProvenanceSchema = z.object({ runtimeId: z.string().min(1), jobId: z.string().min(1), attemptId: z.string().min(1), attemptSequence: z.number().int().min(1), fencingToken: z.string().regex(/^\d+$/), workerInstanceId: z.string().min(1).max(200), workerBuildHash: z.string().regex(HASH_RE), runtimePolicyHash: z.string().regex(HASH_RE), capabilityCode: code, capabilityVersion: z.literal('1.0'), capabilityHash: z.string().regex(HASH_RE), handlerCode: code, handlerVersion: z.literal('1.0'), jobPayloadHash: z.string().regex(HASH_RE), workflowInstanceId: z.string().min(1), workflowDefinitionHash: z.string().regex(HASH_RE), phaseCode: code, phaseEntrySequence: z.number().int().min(1), correctionCycle: z.number().int().min(0), executorAgentId: z.string().min(1), executorAgentCode: code, executorAgentConfigVersion: z.number().int().min(1), executorAgentConfigHash: z.string().regex(HASH_RE), provider: z.literal('mock'), dataMode: z.literal('synthetic') }).strict();
export type AiResultProvenance = z.infer<typeof AiResultProvenanceSchema>;
export const AiResultArtifactDraftSchema = z.object({ resultPayload: z.unknown(), artifacts: z.array(z.object({ ordinal: z.number().int().min(0), slotCode: code, logicalKey, artifactType: code, artifactVersion: z.literal('1.0'), mediaType: z.literal('application/json'), payload: z.unknown(), supersedesArtifactId: z.string().cuid().optional() }).strict()).max(AI_RESULT_LIMITS.maxArtifacts), sourceReferences: z.array(z.object({ sourceArtifactId: z.string().cuid(), sourceArtifactHash: z.string().regex(HASH_RE), role: code, ordinal: z.number().int().min(0) }).strict()).max(AI_RESULT_LIMITS.maxSourceReferences).default([]), retention: z.object({ policyCode: z.literal('AI_RESULT_ARTIFACT_RETENTION_V1'), policyVersion: z.literal('1.0'), retentionClass: z.enum(['AUDIT_SYNTHETIC', 'TEMPORARY_SYNTHETIC']), retainUntil: z.string().datetime({ offset: true }).optional() }).strict() }).strict();
export type AiResultArtifactDraft = z.infer<typeof AiResultArtifactDraftSchema>;
function freezeDeep<T>(value: T): Readonly<T> { if (value && typeof value === 'object') { Object.freeze(value); Object.values(value).forEach(freezeDeep); } return value as Readonly<T>; }
function artifactSchemaHash(artifactType: ArtifactType) { return canonicalSha256({ domain: 'ai.artifactSchema.v1', artifactType, version: '1.0', schema: schemaDescriptions[artifactType] }); }
const contractEntries = FAI_AUDIT_JOB_CODES.map((jobCode) => { const capability = getAiOrchestratorWorkerCapability(jobCode)!; const artifactTypes = [...contractDefs[jobCode]] as ArtifactType[]; const artifactSchemas = Object.fromEntries(artifactTypes.map((t) => [t, { artifactSchemaCode: `${t}_SCHEMA`, artifactSchemaVersion: '1.0', artifactSchemaHash: artifactSchemaHash(t), description: schemaDescriptions[t] }])); const identity = { schemaVersion: 1, catalogCode: AI_RESULT_CONTRACT_CATALOG_CODE, resultContractCode: `FAI_AUDIT_${jobCode}_RESULT`, resultContractVersion: AI_RESULT_CONTRACT_VERSION, jobCode, jobDefinitionHash: FAI_AUDIT_JOB_DEFINITION_HASHES[jobCode], capabilityHash: AI_ORCHESTRATOR_WORKER_CAPABILITY_HASHES[jobCode], handlerCode: capability.handlerCode, handlerVersion: capability.handlerVersion, runtimePolicyHash: AI_ORCHESTRATOR_WORKER_RUNTIME_POLICY_HASH, artifactSchemas }; return [jobCode, freezeDeep({ ...identity, resultContractHash: canonicalSha256({ domain: 'ai.resultContract.v1', ...identity }), requiredArtifactTypes: artifactTypes, resultSchema: artifactTypes.length === 1 ? artifactTypes[0] : 'CORRECTION_RESULT', artifactSchemas })] as const; });
const CONTRACTS = freezeDeep(Object.fromEntries(contractEntries) as unknown as Record<FaiAuditJobCode, Readonly<{ resultContractCode: string; resultContractVersion: '1.0'; resultContractHash: string; requiredArtifactTypes: readonly ArtifactType[]; artifactSchemas: Readonly<Record<string, { artifactSchemaCode: string; artifactSchemaVersion: '1.0'; artifactSchemaHash: string; description: Record<string, string> }>> }>>);
const CONTRACT_BY_JOB = new Map<string, typeof CONTRACTS[FaiAuditJobCode]>(Object.entries(CONTRACTS));
export function getAiResultContract(jobCode: string) { return CONTRACT_BY_JOB.get(jobCode) ?? null; }
export function listAiResultContracts() { return Object.values(CONTRACTS); }
export const AI_RESULT_CONTRACT_CATALOG_HASH = canonicalSha256({ domain: 'ai.resultContractCatalog.v1', contracts: Object.fromEntries(Object.entries(CONTRACTS).map(([k, v]) => [k, { resultContractCode: v.resultContractCode, resultContractVersion: v.resultContractVersion, resultContractHash: v.resultContractHash, requiredArtifactTypes: v.requiredArtifactTypes, artifactSchemas: v.artifactSchemas }])) });
function inspectJson(value: unknown, depth = 1): { nodes: number; maxDepth: number } { if (depth > AI_RESULT_LIMITS.maxJsonDepth) throw new TypeError('AI_RESULT_JSON_TOO_DEEP'); if (value === null || typeof value === 'boolean' || typeof value === 'string') return { nodes: 1, maxDepth: depth }; if (typeof value === 'number') { if (!Number.isFinite(value) || (Number.isInteger(value) && !Number.isSafeInteger(value))) throw new TypeError('AI_RESULT_UNSAFE_NUMBER'); return { nodes: 1, maxDepth: depth }; } if (Array.isArray(value)) return value.reduce<{ nodes: number; maxDepth: number }>((a, v) => { const r = inspectJson(v, depth + 1); return { nodes: a.nodes + r.nodes, maxDepth: Math.max(a.maxDepth, r.maxDepth) }; }, { nodes: 1, maxDepth: depth }); if (typeof value === 'object' && Object.getPrototypeOf(value) === Object.prototype) return Object.values(value as Record<string, unknown>).reduce<{ nodes: number; maxDepth: number }>((a, v) => { const r = inspectJson(v, depth + 1); return { nodes: a.nodes + r.nodes, maxDepth: Math.max(a.maxDepth, r.maxDepth) }; }, { nodes: 1, maxDepth: depth }); throw new TypeError('AI_RESULT_NON_JSON'); }
function assertPayload(value: unknown, limit: number) { const canonical = canonicalJson(value); const bytes = Buffer.byteLength(canonical, 'utf8'); const i = inspectJson(value); if (i.nodes > AI_RESULT_LIMITS.maxJsonNodes) throw new TypeError('AI_RESULT_JSON_TOO_LARGE'); if (bytes > limit) throw new TypeError('AI_RESULT_BYTES_TOO_LARGE'); if (FORBIDDEN.test(canonical)) throw new TypeError('AI_RESULT_FORBIDDEN_CONTENT_REDACTED'); return { bytes, payloadHash: sha256(`ai.payload.v1\n${canonical}`) }; }
function normalizeRetention(retention: AiResultArtifactDraft['retention']) { const retainUntil = retention.retainUntil ? new Date(retention.retainUntil) : null; if (retainUntil && Number.isNaN(retainUntil.getTime())) throw new TypeError('AI_RESULT_RETENTION_INVALID'); const normalizedRetainUntil = retainUntil ? new Date(Math.trunc(retainUntil.getTime())).toISOString() : null; const normalized = { policyCode: retention.policyCode, policyVersion: retention.policyVersion, retentionClass: retention.retentionClass, retainUntil: normalizedRetainUntil }; return { ...normalized, retentionPolicyHash: canonicalSha256({ domain: 'ai.retentionPolicy.v1', ...normalized }) }; }
export function validateAndHashAiResultDraft(jobCode: string, draftInput: unknown, provenanceInput: unknown) { const contract = getAiResultContract(jobCode); if (!contract) throw new TypeError('AI_RESULT_CONTRACT_NOT_FOUND'); const provenance = AiResultProvenanceSchema.parse(provenanceInput); const draft = AiResultArtifactDraftSchema.parse(draftInput); const expected = [...contract.requiredArtifactTypes].sort(); const actual = draft.artifacts.map((a) => a.artifactType).sort(); if (JSON.stringify(expected) !== JSON.stringify(actual)) throw new TypeError('AI_RESULT_ARTIFACT_CARDINALITY_INVALID'); const seen = new Set<string>(); const artifacts = [...draft.artifacts].sort((a, b) => a.ordinal - b.ordinal).map((a, index) => { if (a.ordinal !== index) throw new TypeError('AI_RESULT_ARTIFACT_ORDER_INVALID'); if (seen.has(a.slotCode)) throw new TypeError('AI_RESULT_ARTIFACT_SLOT_DUPLICATE'); seen.add(a.slotCode); const artifactType = a.artifactType as ArtifactType; const parsedPayload = schemas[artifactType]?.parse(a.payload); if (!parsedPayload) throw new TypeError('AI_RESULT_ARTIFACT_SCHEMA_NOT_FOUND'); const h = assertPayload(parsedPayload, AI_RESULT_LIMITS.maxArtifactBytes); const schemaMeta = contract.artifactSchemas[artifactType]; if (!schemaMeta) throw new TypeError('AI_RESULT_ARTIFACT_NOT_ALLOWED'); return { ...a, artifactType, payload: parsedPayload, payloadBytes: h.bytes, payloadHash: h.payloadHash, artifactSchemaCode: schemaMeta.artifactSchemaCode, artifactSchemaVersion: schemaMeta.artifactSchemaVersion, artifactSchemaHash: schemaMeta.artifactSchemaHash, artifactHash: canonicalSha256({ domain: 'ai.artifact.v1', artifactType, ordinal: a.ordinal, slotCode: a.slotCode, logicalKey: a.logicalKey, artifactVersion: a.artifactVersion, mediaType: a.mediaType, artifactSchemaHash: schemaMeta.artifactSchemaHash, payloadHash: h.payloadHash, supersedesArtifactId: a.supersedesArtifactId ?? null }) }; }); const resultPayload = artifacts.length === 1 ? artifacts[0].payload : z.object({ synthetic: z.literal(true), summary: syntheticText, correctedReportHash: z.string().regex(HASH_RE), correctionManifestHash: z.string().regex(HASH_RE) }).strict().parse(draft.resultPayload); const rp = assertPayload(resultPayload, AI_RESULT_LIMITS.maxResultBytes); const sourceReferences = [...draft.sourceReferences].sort((a, b) => a.ordinal - b.ordinal); sourceReferences.forEach((s, i) => { if (s.ordinal !== i) throw new TypeError('AI_RESULT_SOURCE_ORDER_INVALID'); }); const retention = normalizeRetention(draft.retention); const totalPayloadBytes = rp.bytes + artifacts.reduce((n, a) => n + a.payloadBytes, 0); if (totalPayloadBytes > AI_RESULT_LIMITS.maxResultBytes) throw new TypeError('AI_RESULT_TOTAL_BYTES_TOO_LARGE'); const manifestHash = canonicalSha256({ domain: 'ai.manifest.v1', artifactHashes: artifacts.map((a) => a.artifactHash), sourceReferences, retention }); const resultHash = canonicalSha256({ domain: 'ai.result.v1', provenance, resultContractHash: contract.resultContractHash, resultPayloadHash: rp.payloadHash, manifestHash, artifactHashes: artifacts.map((a) => a.artifactHash), sourceReferences }); return { contract, provenance, resultPayload, resultPayloadHash: rp.payloadHash, resultPayloadBytes: rp.bytes, artifacts, sourceReferences, retention, manifestHash, resultHash, resultContractHash: contract.resultContractHash, totalPayloadBytes }; }
export function createSyntheticAiResultDraft(jobCode: FaiAuditJobCode | string): AiResultArtifactDraft { const contract = getAiResultContract(jobCode)!; const payloadFor = (type: ArtifactType) => { switch (type) { case 'DOCUMENT_MANIFEST': return { synthetic: true, summary: 'synthetic summary', documentCount: 1 }; case 'DOCUMENT_CLASSIFICATION': return { synthetic: true, summary: 'synthetic summary', classes: ['synthetic class'] }; case 'EVIDENCE_SET': return { synthetic: true, summary: 'synthetic summary', evidenceItems: ['synthetic evidence'] }; case 'FINANCIAL_ANALYSIS': return { synthetic: true, summary: 'synthetic summary', indicators: [{ code: 'SYN', value: 1 }] }; case 'CREDIT_ANALYSIS': return { synthetic: true, summary: 'synthetic summary', rating: 'synthetic rating', confidence: 1 }; case 'CALCULATION_SET': return { synthetic: true, summary: 'synthetic summary', calculations: [{ code: 'SYN', value: 1 }] }; case 'FINDINGS_DRAFT': return { synthetic: true, summary: 'synthetic summary', findings: ['synthetic finding'] }; case 'REPORT_DRAFT': return { synthetic: true, summary: 'synthetic summary', sections: ['synthetic section'] }; case 'SCHEMA_REVIEW_REPORT': case 'NUMERIC_REVIEW_REPORT': case 'SOURCE_REVIEW_REPORT': case 'RED_TEAM_REVIEW_REPORT': return { synthetic: true, summary: 'synthetic summary', passed: true, issues: [] }; case 'CORRECTED_REPORT': return { synthetic: true, summary: 'synthetic summary', correctedSections: ['synthetic section'] }; case 'CORRECTION_MANIFEST': return { synthetic: true, summary: 'synthetic summary', correctionReasons: ['synthetic reason'], supersededArtifactHashes: [] }; } }; const artifacts = contract.requiredArtifactTypes.map((artifactType, ordinal) => ({ ordinal, slotCode: artifactType, logicalKey: `synthetic.${artifactType.toLowerCase()}`, artifactType, artifactVersion: '1.0' as const, mediaType: 'application/json' as const, payload: payloadFor(artifactType) })); const resultPayload = artifacts.length === 1 ? artifacts[0].payload : { synthetic: true, summary: 'synthetic summary', correctedReportHash: '0'.repeat(64), correctionManifestHash: '1'.repeat(64) }; return { resultPayload, artifacts, sourceReferences: [], retention: { policyCode: 'AI_RESULT_ARTIFACT_RETENTION_V1', policyVersion: '1.0', retentionClass: 'AUDIT_SYNTHETIC' } }; }
