import Link from 'next/link';

const base = 'inline-flex items-center justify-center rounded-2xl px-4 py-2.5 text-sm font-extrabold transition focus:outline-none focus:ring-2 focus:ring-fai-lime focus:ring-offset-2 disabled:cursor-not-allowed';
export function PrimaryButton(props: React.ButtonHTMLAttributes<HTMLButtonElement>) { return <button {...props} className={`${base} bg-gradient-to-r from-fai-blue to-fai-green text-white shadow-lg shadow-fai-blue/15 hover:-translate-y-0.5 hover:shadow-xl disabled:from-slate-200 disabled:to-slate-200 disabled:text-slate-500 ${props.className ?? ''}`} />; }
export function SecondaryLink({ href, children }: { href: string; children: React.ReactNode }) { return <Link className={`${base} border border-fai-blue/15 bg-white/90 text-fai-blue shadow-sm hover:-translate-y-0.5 hover:bg-fai-blue/10`} href={href}>{children}</Link>; }
export function OpenLink({ href, children = 'Apri' }: { href: string; children?: React.ReactNode }) { return <Link className="font-black text-fai-blue underline decoration-fai-lime/60 underline-offset-4 transition hover:text-fai-green" href={href}>{children}</Link>; }
export function DisabledAction({ children, reason = 'Funzione non ancora disponibile' }: { children: React.ReactNode; reason?: string }) { return <span className={`${base} cursor-not-allowed bg-slate-100 text-slate-500 ring-1 ring-slate-200`} title={reason} aria-disabled="true">{children}<span className="sr-only"> — {reason}</span></span>; }
export function Hint({ children }: { children: React.ReactNode }) { return <p className="mt-2 text-xs leading-5 text-slate-500">{children}</p>; }
