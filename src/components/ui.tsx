import Link from 'next/link';
import { logoutAction } from '@/lib/login-actions';
export function Card({ title, children }: { title: string; children: React.ReactNode }) { return <section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="mb-3 text-lg font-semibold text-fai-navy">{title}</h2>{children}</section>; }
export function Stat({ label, value }: { label: string; value: number | string }) { return <div className="rounded-xl bg-white p-4 shadow-sm"><div className="text-sm text-fai-gray">{label}</div><div className="mt-2 text-3xl font-bold text-fai-blue">{value}</div></div>; }
export function Table({ headers, rows }: { headers: string[]; rows: Array<Array<React.ReactNode>> }) { return <div className="overflow-hidden rounded-xl border bg-white"><table className="w-full text-sm"><thead className="bg-slate-50 text-left text-fai-gray"><tr>{headers.map((h) => <th className="px-4 py-3" key={h}>{h}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr className="border-t" key={i}>{r.map((c,j)=><td className="px-4 py-3" key={j}>{c}</td>)}</tr>)}</tbody></table></div>; }
export function Nav() { const items = [['Dashboard','/dashboard'],['Lead','/leads'],['Clienti','/clients'],['Progetti','/projects'],['Documenti','/documents'],['Pre-analisi','/preanalyses'],['Dossier','/dossiers'],['Contratti','/contracts'],['Pagamenti','/payments'],['Task','/tasks'],['AI','/ai']]; return <aside className="min-h-screen w-64 bg-fai-navy p-5 text-white"><div className="mb-8 text-xl font-bold">FAI CRM</div><nav className="space-y-1">{items.map(([l,h])=><Link className="block rounded-lg px-3 py-2 hover:bg-fai-blue" href={h} key={h}>{l}</Link>)}</nav><form action={logoutAction} className="mt-8"><button className="w-full rounded-lg px-3 py-2 text-left text-sm hover:bg-fai-blue" type="submit">Logout</button></form></aside>; }
