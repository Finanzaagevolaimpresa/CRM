export const dynamic = 'force-dynamic';
import { Card, PageHeader, StatusBadge } from '@/components/ui';
import { DisabledAction, Hint, SecondaryLink } from '@/components/actions';
import { prisma } from '@/lib/prisma';
export default async function Page({params}:{params:Promise<{id:string}>}){ const {id}=await params; const dossier=await prisma.dossier.findUnique({where:{id}}); if(!dossier) return <PageHeader title="Dossier non trovato" description="Il record richiesto non esiste o non è più disponibile."/>; const [client,project]=await Promise.all([prisma.client.findUnique({where:{id:dossier.clientId}}),prisma.project.findUnique({where:{id:dossier.projectId}})]); return <div className="space-y-6"><PageHeader title={`Dossier — ${dossier.title}`} description="Contenuto interno del dossier. Export manuale non attivo nel MVP se non collegato a file già generato."/><div className="flex flex-wrap gap-3"><SecondaryLink href="/dossiers">← Torna alla lista</SecondaryLink><DisabledAction>Export PDF</DisabledAction><DisabledAction>Export DOCX</DisabledAction></div><Hint>Funzione prevista, non ancora attiva nel MVP: gli export non vengono generati automaticamente.</Hint><Card title="Dati"><p>Cliente: {client?.displayName??dossier.clientId}</p><p>Progetto: {project?.title??dossier.projectId}</p><p>Tipo: {dossier.type}</p><p>Versione: {dossier.version}</p><p>Stato: <StatusBadge status={dossier.status}/></p></Card><Card title="Contenuto"><div className="whitespace-pre-wrap text-sm text-fai-gray">{dossier.markdownContent??'Nessun dato presente'}</div></Card></div> }
