export const dynamic = 'force-dynamic';
import { Card, EmptyState, PageHeader, StatusBadge, Table } from '@/components/ui';
import { prisma } from '@/lib/prisma';
export default async function Page(){ const outputs = await prisma.aiOutput.findMany({ where:{ status:{in:['needs_review','flagged']} }, orderBy:{createdAt:'desc'} }); return <div className="space-y-6"><PageHeader title="Output AI da revisionare" description="Coda di controllo umano: nessun output AI è approvato o inviabile finché non risulta approvato internamente."/><Card title="Coda revisione">{outputs.length===0?<EmptyState title="Nessun output in attesa">Tutti gli output AI risultano già gestiti o archiviati.</EmptyState>:<Table headers={['Titolo','Stato','Revisione umana','Creato il']} rows={outputs.map(o=>[o.title,<StatusBadge status={o.status} key='s'/>,o.requiresHumanReview?'Obbligatoria':'Non richiesta',o.createdAt.toISOString()])}/>}</Card></div> }
