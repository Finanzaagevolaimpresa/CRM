export const dynamic = 'force-dynamic';
import { Card, EmptyState, PageHeader, StatusBadge, Table, TimestampMeta } from '@/components/ui';
import { SecondaryLink } from '@/components/actions';
import { prisma } from '@/lib/prisma';
import { requirePermission } from '@/lib/auth';
import { canViewProject } from '@/lib/access-control';
export default async function Page({params}:{params:Promise<{id:string}>}){ const session=await requirePermission('project.read'); const {id}=await params; const project=await prisma.project.findUnique({where:{id}}); if(!project) return <PageHeader title="Progetto non trovato" description="Il record richiesto non esiste o non è più disponibile."/>; const client=await prisma.client.findUnique({where:{id:project.clientId}}); if(!canViewProject(session,{...project,client})) return <PageHeader title="Progetto non accessibile" description="Il record richiesto non è disponibile per il tuo profilo."/>; const [expenses,consultant]=await Promise.all([prisma.projectExpense.findMany({where:{projectId:id}}), project.consultantId ? prisma.user.findUnique({where:{id:project.consultantId}}) : null]); return <div className="space-y-6"><PageHeader title={`Progetto — ${project.title}`} description="Scheda progetto reale con importi, stato, voci di spesa e timestamp."/><SecondaryLink href="/projects">← Torna alla lista</SecondaryLink><Card title="Dati progetto"><p>Cliente: {client?.displayName??project.clientId}</p><p>Investimento: {project.totalInvestment?`€ ${Number(project.totalInvestment).toLocaleString('it-IT')}`:'—'}</p><p>Richiesto: {project.requestedAmount?`€ ${Number(project.requestedAmount).toLocaleString('it-IT')}`:'—'}</p><p>Stato: <StatusBadge status={project.status}/></p><p className="mt-2 whitespace-pre-wrap text-sm text-fai-gray">{project.description??'Nessun dato presente'}</p><TimestampMeta createdAt={project.createdAt} updatedAt={project.updatedAt} updatedBy={consultant?.name ?? project.consultantId} /></Card><Card title="Spese progetto">{expenses.length===0?<EmptyState title="Nessun dato presente">Nessuna voce di spesa progetto registrata.</EmptyState>:<Table headers={['Categoria','Descrizione','Importo','Ammissibilità']} rows={expenses.map(e=>[e.category,e.description,`€ ${Number(e.amount).toLocaleString('it-IT')}`,e.potentiallyEligible?'Potenzialmente':'Da verificare'])}/>}</Card></div> }
